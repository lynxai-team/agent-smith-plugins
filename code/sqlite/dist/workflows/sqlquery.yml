tool:
  description: Read data or write in the database. This tool is aware of the database schema and can read and write everything.
  arguments:
    prompt:
        description: The question to ask about data or the data to write or update to the database
steps:
  - adaptater: db-getschema
  - agent: db-create-query
  - action: db-ask-execute-query